services:
  app:
    #build: Dockerfile
    image: recipevault
    env_file:
      - .env.example
    ports:
      - "${APP_PORT:-8000}:8000"
    depends_on:
      - db

  db:
    image: postgres:16-alpine
    env_file:
      - .env.example
